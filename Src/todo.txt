TODO LIST:

- SET REMOVE ELEMENT FIX ITERATOR FROM BEING CONST
- impl set meta class desc
- Rest of container types (just copy from dcarray): Set, List, Deque, Map (Dont copy this one! redo it)
- PropertySet
- Check is the calculated version CRC is correct (do this after PropertySet)

notes:
metastream deletes the datastream passed on open
can only write MSV6, implement more in future (maybe check EXEs)
TTCZ/TTCE are datastreamcontainers (see DataStreamContainerParams):
mpDstStream     Ptr<DataStream>
mpSrcStream     Ptr<DataStream> //telltale ptr type, jsut a normal ptr will do 
mDstOffset      dq
mWindowSize     dd
mbCompress      db
mbEncrypt       db
mCompressionLibrary dd ? ; enum Compression::Library
//db byte dd int32 dq int64 (quad)