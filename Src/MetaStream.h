#ifndef _METASTREAM
#define _METASTREAM

#include "Symbol.h"

class MetaStream {//using ptrs instead of actual objects like in the real engine
private:

public:
	

	void serialize_Symbol(Symbol*);

};

#endif